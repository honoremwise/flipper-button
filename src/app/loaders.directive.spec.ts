import { LoadersDirective } from './loaders.directive';

describe('LoadersDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadersDirective();
    expect(directive).toBeTruthy();
  });
});
